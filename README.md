# Java Console URL Shortener

**Консольное приложение для сокращения ссылок с поддержкой TTL, лимита переходов, DI и конфигурируемыми параметрами. Ваш локальный “clck.ru” с удобной CLI!**

---

## 🚀 Введение

Это локальный сервис генерации коротких ссылок, позволяющий:
- Создавать уникальные короткие ссылки для каждого пользователя (анонимный UUID).
- Задать время жизни и лимит переходов для каждой ссылки.
- Автоматически очищать истёкшие ссылки.
- Получать уведомления об удалении и блокировке.
- Все параметры приложения — из файла конфигурации!

---

## ⚡️ Быстрый старт

### 1. Клонируйте репозиторий
git clone <repo_url>
cd java-console-url-shortener

### 2. Подготовьте конфигурацию
Измените файл `application.properties` в корне проекта под Ваши предпочтения:
shortener.ttl.hours=24
shortener.maxClicks=5
shortener.cleanup.period.minutes=30
shortener.domain=http://localhost:8080/u/

---

## 🛠️ Команды CLI

create <url> [--limit N] [--ttl-hours H] — создать короткую ссылку
open <id> — открыть короткую ссылку в браузере
list — показать все ваши ссылки
delete <id> — удалить свою короткую ссылку
set-limit <id> <N> — изменить лимит переходов по ссылке
set-ttl <id> <hours> — изменить время жизни ссылки
help — справка по командам
exit — выход из приложения

### Примеры
create https://ya.ru
create https://skillfactory.ru --limit 10 --ttl-hours 48
open 04AbzC9X
list
delete 04AbzC9X

---

## 🧩 Конфигурация

Все ключевые параметры читаются из файла `application.properties`:

| Параметр                             | Описание                                        |
|--------------------------------------|-------------------------------------------------|
| `shortener.ttl.hours`                | Время жизни ссылки в часах (по умолчанию)       |
| `shortener.maxClicks`                | Лимит переходов по короткой ссылке (по умолч.)  |
| `shortener.cleanup.period.minutes`   | Интервал фоновой очистки истёкших ссылок        |
| `shortener.domain`                   | Базовый домен для генерации кликабельных URL    |

Измените конфиг — и приложение автоматически применит новые значения при следующем запуске.

---

## 🔋 Архитектура

- **DI (Dependency Injection):** Все зависимости (репозиторий, сервисы, notifier) передаются через конструктор.
- **Уведомления:** Вся информация о блокировке/удалении ссылок выводится в консоль (`[NOTIFY] ...`). Легко расширяется под email/webhook.
- **UUID пользователя:** Каждый пользователь получает уникальный идентификатор при первом запуске; все его ссылки отслеживаются по этому UUID (без логина).

---

## 🌐 Локальный мини-сервер

Для работы коротких ссылок в формате http://localhost:8080/u/<id> запускается локальный HTTP‑редирект. Клик по ссылке из списка вызывает переход к исходному URL с учётом TTL и лимита!
